#cloud-config
autoinstall:
  version: 1
  
  # 1. Configuração do Usuário
  locale: pt_BR.utf8
  keyboard:
    layout: br
  timezone: "America/Sao_Paulo" 

  # 2. Configuração de Rede
  packages:
    - git
    - vim
    - wget
    - curl
    - apt-transport-https
    - ca-certificates
    - gnupg
    - dotnet-sdk-8.0

  # 3. Configuração de Software Adicional
  codecs:
    install: true
  drivers:
    install: true

  updates: all

  # 4. Instalação de Snaps
  snaps:
    - name: postman
      channel: stable
    - name: rider
      channel: stable
      classic: true

  # Estas linhas dizem ao instalador para NÃO automatizar estas telas
  interactive-sections:
    - identity
    - storage
    - network
